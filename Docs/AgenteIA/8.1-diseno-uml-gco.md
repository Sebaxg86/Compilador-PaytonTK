# Diseño UML de `GenCodigoObj`

Este documento contiene el diseño UML de la clase `GenCodigoObj`, correspondiente a la etapa de **Generación de Código Objeto** del compilador PaytonTK.

`GenCodigoObj` es la clase responsable de traducir la tabla de cuádruplos a código objeto en ensamblador. Consume los cuádruplos generados previamente, identifica variables y temporales, emite la plantilla ASM y traduce asignaciones simples y operaciones binarias al subconjunto soportado.

## Diagrama UML

```mermaid
classDiagram
    class GenCodigoObj {
        -String AX
        -String BX
        -String FIRMA_PREFIJO
        -String AMBITO_FUNCION
        -String AMBITO_PARAM_PREFIJO
        -Compilador cmp
        -ArrayList~Cuadruplo~ cuadruplos
        -LinkedHashSet~String~ nombresVariables
        -LinkedHashSet~String~ nombresTemporales

        +GenCodigoObj(Compilador c)
        +generar() void

        -genEncabezadoASM() void
        -genDeclaraVarsASM() void
        -genSegmentoCodigo() void
        -genPieASM() void
        -algoritmoGCO() void
        -prepararMetadata() void
        -registrarTemporal(String operando) void
        -traducirAsignacionSimple(Cuadruplo cuadruplo) void
        -traducirAsignacionBinaria(Cuadruplo cuadruplo) void
        -emitirImpresionVariables() void
        -mostrar(String linea) void
        -errorCodObj(String mensaje) void
        -nombreDesdeOperando(String operando) String
        -operandoEnteroValido(String operando) boolean
        -esReferenciaTS(String operando) boolean
        -entradaDeReferencia(String operando) int
        -esTemporal(String nombre) boolean
        -esInmediato(String operando) boolean
        -esOperacionBinaria(String op) boolean
        -esFirmaFuncion(String tipo) boolean
    }

    class Compilador
    class Cuadruplo
    class Linea_TS

    GenCodigoObj --> Compilador : usa
    GenCodigoObj --> Cuadruplo : consume
    GenCodigoObj ..> Linea_TS : consulta tabla de simbolos
```

## Responsabilidad de la clase

La clase `GenCodigoObj` realiza las siguientes tareas principales:

- asegura que existan cuádruplos antes de generar ASM;
- obtiene la lista de cuádruplos desde `cmp.cua`;
- prepara variables y temporales que serán declarados en `.data`;
- genera el encabezado y cierre del programa ensamblador;
- traduce cuádruplos de asignación simple;
- traduce cuádruplos de operaciones binarias `+`, `-` y `*`;
- envía cada línea de ASM a la interfaz por medio de `iuListener`.
